const getBotLocation = () => webConfig.botLocation;
const getBotSecretKey = () => webConfig.secretKey;
const getAPI = () => webConfig.api;

export default {
  getBotLocation,
  getBotSecretKey,
  getAPI,
};
